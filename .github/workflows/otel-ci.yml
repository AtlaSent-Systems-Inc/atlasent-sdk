name: otel-preview CI

# Adapter sub-package CI — see CI_GAPS_AUDIT.md.
# Mirrors v2-alpha-ci.yml's per-language structure.

on:
  push:
    branches: [main]
    paths:
      - "typescript/packages/otel/**"
      - "python/atlasent_otel_preview/**"
      - ".github/workflows/otel-ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "typescript/packages/otel/**"
      - "python/atlasent_otel_preview/**"
      - ".github/workflows/otel-ci.yml"

jobs:
  typescript:
    name: TypeScript @atlasent/otel
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ["20.x", "22.x"]
    defaults:
      run:
        working-directory: typescript/packages/otel
    steps:
      - uses: actions/checkout@v6

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm install

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

  python:
    name: Python atlasent-otel-preview
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    defaults:
      run:
        working-directory: python/atlasent_otel_preview
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with ruff
        run: ruff check .

      - name: Run tests
        run: pytest tests/ -v
