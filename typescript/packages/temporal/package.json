{
  "name": "@atlasent/temporal-preview",
  "version": "2.0.0-preview.0",
  "description": "Preview Temporal adapter for AtlaSent. Wraps Temporal Activities with protect() so each invocation produces an audit-chain entry. DO NOT USE IN PRODUCTION.",
  "license": "Apache-2.0",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@atlasent/sdk": "^1.4.0",
    "@temporalio/activity": "^1.0.0",
    "@temporalio/workflow": "^1.0.0"
  },
  "peerDependenciesMeta": {
    "@temporalio/activity": {
      "optional": false
    },
    "@temporalio/workflow": {
      "optional": true
    }
  },
  "devDependencies": {
    "@atlasent/sdk": "file:../../",
    "@temporalio/activity": "^1.10.0",
    "@temporalio/workflow": "^1.10.0",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  },
  "keywords": [
    "atlasent",
    "temporal",
    "v2-preview"
  ]
}
