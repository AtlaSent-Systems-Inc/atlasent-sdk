// AtlaSent Go SDK — execution-time authorization for AI agents.
//
// Status: scaffold. This module establishes the import path and
// minimal surface so downstream consumers can pin a version while
// the implementation lands. Method signatures mirror the TS + Python
// SDKs for cross-language parity (see ../contract/SDK_COMPATIBILITY.md).

module github.com/AtlaSent-Systems-Inc/atlasent-sdk/go

go 1.22
